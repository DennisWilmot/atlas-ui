# Inline CTAs

Disposition: EXAMPLE_ONLY

This folder contains documentation-only marketing CTA references for Storybook.
It is not imported by `src/primitives/` or `src/patterns/` and is not exported
from the package.

The example uses neutral copy, no fake users, no fake emails, no fake URLs, no
app-specific routes, and no hardcoded business metrics.
