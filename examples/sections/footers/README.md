# Section Footers Example

This directory quarantines the legacy `section-footers` audit item as `EXAMPLE_ONLY`.

- Storybook title: `Examples/Sections/Footers`
- Public export: none
- Treatment: neutral reference only
- Data policy: no users, emails, URLs, business metrics, app routes, or fixed row actions

The example demonstrates footer placement for section-level supporting notes and injected actions. It must not be imported by `src/primitives`, `src/patterns`, or package entry files.
