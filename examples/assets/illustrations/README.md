# Illustration Assets

Disposition: `EXAMPLE_ONLY`

Status: `EXAMPLE_ONLY_DONE`

These clean-room illustration assets are example references only. They are not part of the npm package exports and must not be imported by files in `src/primitives/` or `src/patterns/`.

Rules:

- Keep illustration files under `examples/assets/illustrations/`.
- Use neutral, non-domain-specific visuals.
- Do not include fake users, fake emails, fake URLs, hardcoded metrics, or app-specific routes.
- Treat legacy illustration source as visual reference only; do not copy it into Atlas UI.
