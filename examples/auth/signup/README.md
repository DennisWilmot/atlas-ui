# Signup Page Example

Disposition: `EXAMPLE_ONLY`

Status: `EXAMPLE_ONLY_DONE`

This directory quarantines signup page treatment as Storybook-only reference material. It is not part of the package exports and must not be imported into public primitives or patterns.

Example data stays neutral: no fake users, fake emails, fake URLs, hardcoded metrics, demo invite flows, or app-specific routes.

Social/provider buttons remain example-only. Real provider identity, icons, OAuth routing, and analytics belong to the consuming app.
